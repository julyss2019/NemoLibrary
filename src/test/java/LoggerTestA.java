import com.github.julyss2019.nemolibrary.bukkit.api.NemoLibraryAPI;
import com.github.julyss2019.nemolibrary.bukkit.common.logger.Logger;
import org.bukkit.command.ConsoleCommandSender;
import org.bukkit.plugin.Plugin;
import org.junit.jupiter.api.Test;

import static org.mockito.Mockito.*;

public class LoggerTestA {
    @Test
    public void test() {
/*        Plugin plugin = mock(Plugin.class);

        when(plugin.getName()).thenReturn("Test");
        Logger logger = NemoLibraryAPI.getLoggerManager().createLogger(plugin);
        ConsoleCommandSender consoleCommandSender = mock(ConsoleCommandSender.class);


        when(logger.getConsoleSender()).thenReturn(consoleCommandSender);
        logger.debug("test {0} {1}", "arg1", "arg2");*/
//        verify(logger.debug();)
    }
}
